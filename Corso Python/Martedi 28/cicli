numeri = [1, 2, 3, 4, 5]

for x in numeri:
    print(x)
    
    numeri2 = range(5)
    for pippo in numeri2:
        print(pippo)
        
        x =int(input())
        for pippo in range(x):
            print(pippo)